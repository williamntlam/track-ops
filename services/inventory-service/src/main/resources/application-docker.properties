# Docker Configuration for Inventory Service
spring.application.name=inventory-service

# Database Configuration - Inventory Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://trackops-postgres-inventory:5432/trackops_inventory}
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Redis Configuration
# Note: Environment variables SPRING_REDIS_HOST and SPRING_REDIS_PORT will override these values
spring.redis.host=trackops-redis
spring.redis.port=6379
spring.redis.timeout=2000ms
spring.redis.database=1

# Kafka Configuration
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:trackops-kafka:9092}
spring.kafka.consumer.group-id=inventory-service
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# Schema Registry Configuration
schema.registry.url=${SCHEMA_REGISTRY_URL:http://trackops-schema-registry:8085}
schema.registry.basic.auth.user.info=${SCHEMA_REGISTRY_BASIC_AUTH_USER_INFO:}
schema.registry.cache.capacity=1000
schema.registry.auto.register.schemas=true
schema.registry.compatibility.level=BACKWARD

# Inventory Service Configuration
inventory.service.retry.max-attempts=3
inventory.service.retry.delay-ms=1000
inventory.service.reservation.timeout-seconds=30

# Server Configuration
server.port=8082

# Health Check Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.health.defaults.enabled=true
management.health.db.enabled=true
management.health.redis.enabled=true